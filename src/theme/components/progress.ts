import type { ComponentSingleStyleConfig } from "@chakra-ui/react"

const progress: ComponentSingleStyleConfig = {
  baseStyle: {
    filledTrack: {
      bg: "#725BFF",
    },
  },
}

export default progress
